# Install the Catalogue Generator

Install the package with pip:

```sh
pip install nomad-plugins
```

For local development, install the project from the repository and run commands through
the configured development environment.
